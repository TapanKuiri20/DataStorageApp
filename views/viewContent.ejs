<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Images</title>
    <link rel="stylesheet" href="/css/viewContents.css">
</head>
<body>
    <h1>hello name</h1>
    <h1><%= text %></h1>

    <% if (photos.length > 0) { %>
        <div class="container">
            <% photos.forEach((photo) => { %> 
                <%= photo._id %>
                <div>
                    <img src="data:<%= photo.contentType %>;base64,<%= photo.data.toString('base64') %>"/>
                </div>
            <% }); %>
        </div>
    <% } else { %>
        <h1>Photo not found</h1>
    <% } %>
</body>
</html>
